import styled from 'styled-components';
import { DropdownItem } from '@cashfree-intl/coherent';

export const StyledDropdownItem = styled(DropdownItem)`
  &&&.item {
    padding: 0.5rem 1rem !important;
  }
`;
